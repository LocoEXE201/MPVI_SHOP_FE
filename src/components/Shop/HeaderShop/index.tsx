"use client";
import "./index.scss";
import HeaderShopComponent from "./HeaderShopComponent";

const HeaderShop = (props: {}) => {
  return (
    <HeaderShopComponent />
  );
};

export default HeaderShop;
